extracts the directory it's in then uses folders with images in them to create PDFs.
ignores files without images(New version).
Loops until closed.
no bulk folders.
ignores files in folders in folder selected.
